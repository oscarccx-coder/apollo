# Update scripts

Put version migrations, updater bootstrap scripts, rollback helpers and release verification scripts here. Generated update ZIPs and downloaded payloads belong in Apollo runtime storage, not Git.
